{
  "name": "od-ai-content",
  "version": "0.1.0",
  "private": true,
  "description": "Local development environment for the OD AI Content WordPress plugin.",
  "scripts": {
    "env:start": "wp-env start",
    "env:stop": "wp-env stop",
    "env:status": "wp-env status",
    "env:logs": "wp-env logs",
    "env:cli": "wp-env run cli wp",
    "test:php": "wp-env run tests-cli --env-cwd=wp-content/plugins/od-ai-content php ./vendor/bin/phpunit -c phpunit.xml.dist"
  },
  "devDependencies": {
    "@wordpress/env": "^11.12.0"
  }
}
